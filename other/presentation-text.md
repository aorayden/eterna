### 1. Вступление
**Слайд:** `1. Титульный (Eterna: приложение-каталог...)`

*   **Спикер 1:** "Мы представляем **Eterna**. Это приложение-каталог для коллекционеров искусства. Моей задачей было спроектировать удобный интерфейс и продумать весь пользовательский опыт (UX), чтобы коллекцией было приятно пользоваться."
*   **Спикер 2:** "А моей задачей было оживить этот дизайн в коде. Приложение написано на **Flutter**, оно полностью рабочее, хранит данные и запускается на Android."

---

### 2. Процесс (Самый динамичный момент)
**Слайд:** `2. От Figma до кода (Схема со стрелочками)`

*   **Спикер 1:** "Разработку мы начали не с кода, а с дизайн-системы."

**Слайд:** `3. Дизайн-система в Figma (Где показаны Типографика, Цвета, Иконки)`

*   **Спикер 1 (продолжает):** "В Figma я разбил проект на атомы: вынес отдельно шрифтовые стили, цветовую палитру и компоненты. Я подготовил макеты так, чтобы разработчику не пришлось гадать над отступами."

**Слайд:** `4. UI Kit в коде (Скриншот VS Code)`

*   **Спикер 2:** "Благодаря такой структуре в Figma, я смог быстро создать **UI Kit** в коде. Я не хардкодил стили, а перенес их в переменные Flutter. Кнопки, инпуты и карточки, которые нарисовал Тимур, стали переиспользуемыми виджетами."

---

### 3. Интерфейс
**Слайд:** `5. Интерфейс галереи (Modal Bottom Sheet)`

*   **Спикер 1:** "Главный экран я спроектировал максимально чистым, чтобы акцент был на картинах. Для удобства добавил поиск и фильтры по эпохам. Интересное решение в детальном просмотре: вместо открытия новой страницы я использовал **Modal Bottom Sheet** (шторку). Это позволяет пользователю быстро просматривать детали, не теряя контекст списка. **А как это реализовано технически — расскажет Ильнур.**"

*(Слайд 6 "Все окна приложения" можно пропустить или быстро пролистать за 2 секунды как перебивку).*

---

### 4. Функционал
**Слайд:** `7. Работа с данными (Ручной ввод / Импорт файла)`

*   **Спикер 2:** "Переходим к «начинке». Чтобы приложение не было просто красивой картинкой, я реализовал полноценную работу с данными.
    Экран импорта поддерживает два режима.
    1. **Ручной ввод:** тут я прикрутил контроллеры и валидацию полей.
    2. **Импорт файла:** использовал нативный пикер. Приложение парсит JSON и массово заливает данные в базу."

**Слайд:** `8. Экспорт и обмен данными (01, 02, 03 Share Plus)`

*   **Спикер 2:** "Также я реализовал логику экспорта.
    Мы используем пакет `share_plus`. Когда вы нажимаете кнопку, приложение собирает данные из базы, генерирует JSON-файл и вызывает системное окно «Поделиться».
    Тут же работает фильтрация: я написал алгоритм, который отбирает картины по жанрам и датам перед выгрузкой."

---

### 5. Техническая часть и финал
**Слайд:** `9. Архитектура приложения (Круговая диаграмма)`

*   **Спикер 2:** "Архитектурно проект построен на **Singleton-сервисе**.
    Я использовал `ValueNotifier` для управления состоянием — это дает мгновенный отклик интерфейса.
    Данные сохраняются асинхронно в локальный файл (как показано на схеме). То есть приложение полностью автономно и работает офлайн."

**Слайд:** `10. Законченный продукт (Телефон в руке)`

*   **Спикер 1:** "В итоге у нас получился законченный продукт. Макеты из Figma перенесены в код с точностью до пикселя."
*   **Спикер 2:** "А под капотом — чистая архитектура и рабочий функционал. У нас всё, готовы ответить на вопросы!"

---

## 🎤 Текст для Спикера 1 (Тимур) — Дизайн и UX

### Блок 1. Вступление (Слайд 1)
> **(Слайд 1: Eterna: приложение-каталог...)**
> "Всем привет! Меня зовут Тимур, и я отвечал за дизайн.
> Мы представляем **Eterna**. Это приложение-каталог для коллекционеров искусства.
> Моей задачей было спроектировать удобный, продуманный интерфейс и весь пользовательский опыт (UX), чтобы коллекцией было приятно пользоваться.
> *(Ждёт пока Спикер 2 закончит.)*"

### Блок 2. Процесс (Слайды 2, 3, 4)
> **(Слайд 2: От Figma до кода)**
> "Разработку мы начали не с кода, а с дизайн-системы.
> В Figma я разбил проект на атомы: вынес отдельно шрифтовые стили, цветовую палитру и компоненты."
>
> **(Слайд 3: Дизайн-система в Figma)**
> "Я подготовил макеты так, чтобы разработчику не пришлось гадать над отступами. **Все стили** и **компоненты** структурированы по страницам, и это наш главный залог качества."

### Блок 3. Интерфейс (Слайд 5)
> **(Слайд 5: Интерфейс галереи)**
> "Главный экран я спроектировал максимально чистым, чтобы акцент был на произведениях. Для удобства добавил поиск и фильтры по эпохам.
> Интересное UX-решение в детальном просмотре: вместо открытия новой страницы я использовал **Modal Bottom Sheet** (шторку). Это позволяет пользователю быстро просматривать детали, не теряя контекст списка.
> **А как это реализовано технически — расскажет Ильнур.**"

### Блок 4. Финал (Слайд 10)
> **(Слайд 10: Законченный продукт)**
> "В итоге у нас получился законченный продукт. Макеты из Figma перенесены в код с точностью до пикселя, и мы выполнили все требования по дизайну."

---

## 🎤 Текст для Спикера 2 (Ильнур) — Код и Логика

### Блок 1. Вступление (Слайд 1)
> **(Слайд 1: Eterna: приложение-каталог...)**
> "А моей задачей было оживить этот дизайн в коде. Приложение написано на **Flutter**, оно полностью рабочее, хранит данные и запускается на Android. *(Ждёт пока Спикер 1 закончит.)*"

### Блок 2. Процесс (Слайды 4, 7, 8, 9)
> **(Слайд 4: UI Kit в коде)**
> "Благодаря такой структуре в Figma, я смог быстро создать **UI Kit** в коде. Я не хардкодил стили, а перенес их в переменные Flutter. Кнопки, инпуты и карточки, которые нарисовал Тимур, стали переиспользуемыми виджетами."

> **(Слайд 7: Работа с данными)**
> "Переходим к «начинке». Чтобы приложение не было просто красивой картинкой, я реализовал полноценную работу с данными.
> Экран импорта поддерживает два режима.
> 1. **Ручной ввод:** тут я прикрутил контроллеры и валидацию полей.
> 2. **Импорт файла:** использовал нативный пикер. Приложение парсит JSON и массово заливает данные в базу."

> **(Слайд 8: Экспорт и обмен данными)**
> "Также я реализовал логику экспорта.
> Мы используем пакет `share_plus`. Когда вы нажимаете кнопку, приложение собирает данные из базы, генерирует JSON-файл и вызывает системное окно «Поделиться».
> Тут же работает фильтрация: я написал алгоритм, который отбирает картины по жанрам и датам перед выгрузкой."

> **(Слайд 9: Архитектура приложения)**
> "Архитектурно проект построен на **Singleton-сервисе**.
> Я использовал `ValueNotifier` для управления состоянием — это дает мгновенный отклик интерфейса.
> Данные сохраняются асинхронно в локальный файл. То есть приложение полностью автономно и работает офлайн."

### Блок 3. Финал (Слайд 10)
> **(Слайд 10: Законченный продукт)**
> "А под капотом — чистая архитектура и рабочий функционал. У нас всё, готовы ответить на вопросы!"